Git 提交记录说明

说明：前几次提交信息用了英文，是因为我开始练习 Git 时按常见的英文 commit message 写法来提交。下面按实际开发顺序整理每次提交完成的内容，方便检查代码是一步一步写出来的，不是最后一次性上传。

e967038 Add student model and roster data
先建立学生类和名单文件。这个阶段只做最基础的数据准备，先让程序里有 Student 对象和学生名单。

4e31976 Implement exam system core features
完成 ExamSys 的主要功能，包括读取名单、查询学生、随机点名、生成考场安排表和准考证。这里是项目主体代码。

04507d9 Add menu entry point
增加 main.py 程序入口，只负责创建系统对象并启动菜单。这样 main.py 比较干净，功能代码都放在 exam_system.py。

6c1d785 Add generated exam arrangement samples
运行程序生成示例结果，把考场安排表和准考证文件夹加入仓库，满足作业要求里的“程序运行后生成的示例结果文件”。

12ecd0f Add collaboration report and usage guide
补充 README，写运行方法、人机协作报告和结果案例。这个阶段主要整理文档。

ac1a69d Add terminal test output record
把终端测试输出保存成运行结果记录，方便后续制作运行结果 PDF。

9a850c9 Refresh generated exam samples after validation
测试后发现生成文件需要和当前名单保持一致，所以重新生成了一遍考场安排表和准考证。

357044a Add git commit history record
新增本文件，用来说明每次提交对应的开发内容。

f4fa48a Update README repository link
创建 GitHub 仓库后，把 README 中的仓库链接补上。

dc22e4a Refresh git history record with repository link update
README 链接修改后，同步更新提交记录说明。

3e43a8c Make collaboration report more specific and personal
把 README 里比较模板化的协作报告改得更具体，加入自己拆任务、测试和修改代码的过程。

b22b5c2 Refresh git history after README revision
README 修改后更新提交记录说明，保持文档和仓库历史一致。

7401813 Polish report wording and add personal code comments
继续修改 README 表述，并在代码里补充中文注释，让代码更容易看懂。

1f25ce8 Update git history record after polish
代码注释和 README 润色后，再次同步提交记录说明。

8d4f734 Clean stale admission ticket files before regeneration
发现重新生成准考证时，如果名单人数变化，旧文件可能残留，所以加了清理旧准考证文件的逻辑。

bf1fdab Refresh git history after final requirement pass
根据作业要求做了一轮检查后，更新提交记录说明。

55b8745 Update local generated outputs and README examples
重新运行程序，更新考场安排、准考证示例和 README 里的输出案例。

49d09e6 Refresh git history record after local update
本地生成文件和 README 变动后，同步更新本记录文件。

6e152fe Show terminal inputs in run result record
修改运行结果记录，让里面能看到输入的功能编号、学号和点名人数。这样运行结果更像真实终端交互。

8bcd1a3 Refresh git history after run transcript update
运行结果记录更新后，同步更新提交记录说明。

cbda084 Add Chinese explanations for git history
给英文 commit message 补中文解释，避免检查时只看到英文看不出具体做了什么。

4705de6 根据最新学生名单更新项目数据
按照最新学生名单重新改数据文件，并调整名单解析逻辑，保证新表格格式也能读取。

0ee7a84 更新最新名单改动的提交说明
最新名单相关内容改完后，把提交记录说明补齐。

31683dd Strengthen robustness for invalid inputs
做鲁棒性完善：处理菜单输入错误、空学号、点名人数非法、名单为空或格式错误、旧考场安排表无效等情况。

f7df38d Update git history after robustness pass
鲁棒性完善后，最后同步本提交记录文件。
